#include <ctype.h>

/**
 * _isdigit - Checks for a digit
 * @c: Argument to be checked
 *
 * Return: 1 if success, 0 otherwise
 **/

int _isdigit(int c)
{
	if (isdigit(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
