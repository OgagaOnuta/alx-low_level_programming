#include <string.h>
#include "main.h"

/**
 * _strlen - Calculates the length of a string
 * @s: String
 *
 * Return: Length of string
 */

int _strlen(char *s)
{
	int xLength = strlen(s);

	return (xLength);
}
